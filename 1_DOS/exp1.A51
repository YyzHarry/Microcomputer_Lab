WDTCN DATA 0FFH
ORG 0000H
AJMP START
ORG 0100H
START:
	MOV WDTCN, #0DEH
	MOV WDTCN, #0ADH
	MOV R0, #20H
	MOV DPTR, #CONST
	CLR A
	MOVC A, @A+DPTR
	MOV R1, A
	CLR A
	INC DPTR
	MOVC A, @A+DPTR
	MOV R2, A
	MOV DPTR, #0000H
	MOVX A, @DPTR
	ADD A, R1
	DA A
	MOV @R0, A
	INC DPTR
	INC R0
	MOVX A, @DPTR
	ADDC A, R2
	DA A
	MOV @R0, A
	INC R0
	CLR A
	MOV ACC.0, C
	MOV @R0, A
	SJMP $
CONST:
	DW 1234H
	END
